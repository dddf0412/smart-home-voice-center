# 智能家居语音环境中心 — 设计文档

## 1. 项目概述

本项目设计并实现了一款基于 **51 单片机（STC89C52RC）** 与 **ESP8266** 的智能家居语音环境中心，完整覆盖物联网「**感知层—网络层—应用层**」三层架构，实现了环境监测、语音交互、自动环境调节、异常报警与远程控制的一体化。

**业务逻辑**（系统做什么）：

- **环境监测**：DHT11 温湿度、XPT2046 光敏、HC-SR04 超声波三路传感器每 2 秒采集一次，LCD1602 本地显示，并经 ESP8266 上报巴法云；
- **语音交互**：按键触发录音 → 讯飞云端语音识别（ASR）→ 关键词意图解析 → 执行控制指令 + 讯飞语音合成（TTS）播报，实现"能听会说"的对话闭环；
- **自动环境调节**：自动模式下光控灯（光照 <30% 开灯、>70% 关灯）与温湿度控风扇（温度 >30℃ 或湿度 >80% 开风扇、带滞回）联动；
- **异常报警**：温湿度越界、灯故障、距离过近任一触发，蜂鸣器报警；
- **远程控制**：网页控制台（实时曲线 + 实体按钮）与微信小程序「巴法云」均可远程查看数据、下发指令。

**核心技术亮点**：

- **语音 ASR/TTS 闭环**：讯飞云端识别与合成，本地关键词意图解析，多回复随机播报；
- **闭环反馈**：自动模式执行动作后用光敏传感器验证结果（感知—执行—验证）；
- **多控制源统一**：语音、网页、小程序三种控制源共用 51 同一套执行逻辑；
- **三层架构**：感知层（51）— 网络层（ESP8266 + MQTT）— 应用层（讯飞云 / 巴法云 / 网页 / 小程序）。

## 2. 系统总体架构

```
┌─────────────────────────── 应用层 ───────────────────────────┐
│   讯飞云(ASR 语音识别 + TTS 语音合成)                         │
│   巴法云(MQTT 数据上传 + 远程控制)                            │
│   网页控制台 + 微信小程序「巴法云」远程查看 / 下发控制指令       |
└──────────────┬────────────────────────┬──────────────────────┘
               │ 语音流(HTTP/WebSocket)   │ MQTT(TCP :9501)
┌──────────────┴────────────────────────┴──────────────────────┐
│                     ESP8266 (网络层 + 语音交互)                |
│   INMP441 麦克风录音 → 讯飞 ASR      MAX98357A DAC+喇叭 ← TTS  |
│   按键触发说话   |   MQTT 连巴法云    |   UART 与 51 通信       |
└───────────────────────────────┬──────────────────────────────┘
                                │ UART (串口, 电平匹配)
┌───────────────────────────────┴──────────────────────────────┐
│                    LY-51S 单片机 (感知层 + 执行层)             │
│   DHT11 温湿度 + 光敏电阻采集 → LCD1602 显示                    │
│   继电器(灯) + 28BYJ-48 步进电机(风扇) + 蜂鸣器报警             │
└───────────────────────────────────────────────────────────────┘
```

## 3. 硬件清单（实际已购，含具体型号）

| 器件 | 具体型号 | 用途 |
|------|---------|------|
| LY-51S 开发板 | 新版（STC89C52RC @ 12MHz） | 主控；板载 ULN2003(7 路)、继电器×2、蜂鸣器、光敏电阻、XPT2046 ADC(SPI)、24C02 |
| ESP8266 开发板 | NodeMCU V3（HW-625B，带 USB） | 网络通信 + 语音交互 |
| LCD1602 | — | 本地显示温湿度/光照/状态 |
| DHT11 温湿度传感器 | 外接模块 | 温度/湿度采集 |
| HC-SR04 超声波 | 外接模块 | 距离探测（近距离报警） |
| INMP441 I2S 麦克风 | 外接模块 | 语音输入 |
| I2S 功放 | HENOPI-98357 V3.3（兼容 MAX98357A，自带喇叭） | TTS 语音播放 |
| 28BYJ-48 步进电机 | 由板载 ULN2003 out2~5 驱动 | 模拟风扇（自动模式温湿度联动） |

**开发环境**：51 用 Keil C51；ESP8266 用 Arduino IDE。

### 3.1 引脚定义

#### LY-51S（STC89C52RC @ 12MHz）

| 功能 | 引脚 | 说明 |
|------|------|------|
| LCD1602 DB0~DB7 | P0.0~P0.7 | 8 位数据线（P0 有排阻上拉） |
| LCD1602 RS / RW / E | P2.4 / P2.5 / P2.6 | RW 恒写 0 |
| UART TXD → ESP8266 | P3.1 | 加分压 5V→3.3V（大阻值 10k+20k） |
| UART RXD ← ESP8266 | P3.0 | 3.3V 直连 |
| DHT11 数据 | P1.0 | |
| 蜂鸣器（ULN2003 in1） | P1.1 | 高电平响（报警只响蜂鸣器） |
| 步进电机/风扇（ULN2003 in2~5） | P1.2~P1.5 | 高电平该相导通 |
| 继电器-灯（ULN2003 in6） | P1.6 | 高电平吸合 |
| 备用（ULN2003 in7） | P1.7 | 未用 |
| 超声波 TRIG / ECHO | P2.2 / P2.3 | HC-SR04 测距 |
| XPT2046 CLK / CS / DIN / DOUT | P3.2 / P3.3 / P3.4 / P3.5 | SPI ADC，读通道0(光敏) |

#### NodeMCU V3（ESP8266）

| 外设 | 引脚 |
|------|------|
| INMP441 SCK / WS / SD | D7(GPIO13) / D5(GPIO14) / D6(GPIO12) |
| INMP441 L/R / VDD | GND / 3V3 |
| 功放 BCLK / LRC / DIN | D8(GPIO15) / D4(GPIO2) / RX(GPIO3) |
| 功放 VIN / SD / GAIN | VIN(5V) / 3V3 / 悬空(9dB) |
| 语音触发按键 | D3(GPIO0)（低电平有效） |
| 51 通信 RX / TX（软串口） | D2(GPIO4) / D1(GPIO5) |
| 调试打印 | TX(GPIO1) |

> 注：功放 GAIN 悬空 = 9dB（默认）；接 GND = 3dB（非 15dB）。INMP441 L/R 接 GND 选左声道。
>
> 供电：两板各用一根 USB 线接电脑（独立供电），需用杜邦线共地。

## 4. MCU 职责分工

### 4.1 LY-51S（8051）— 感知 + 执行

- 周期读取 DHT11 温湿度、光敏电阻光照强度（经 XPT2046 SPI ADC）、超声波距离
- LCD1602 显示温湿度、光照、灯态、自动模式状态
- 通过 UART 向 ESP8266 上报传感器数据（D/L/A/R/W/F 六帧）
- 接收 ESP8266 下发的控制指令，驱动继电器（灯）与 28BYJ-48 步进电机（风扇）
- 自动环境调节逻辑（见 5.3）：光控灯 + 温湿度控风扇
- 温湿度/光强/距离超阈值触发蜂鸣器报警（报警只响蜂鸣器）

### 4.2 ESP8266 — 网络 + 语音

- 语音闭环：按键触发录音 → 讯飞 ASR → 意图解析 → 执行动作 + 讯飞 TTS → 播放
- MQTT：连接巴法云，发布环境数据、订阅控制指令
- UART：与 51 通信，转发控制指令、接收传感器数据

## 5. 核心功能设计

### 5.1 语音交互（核心特色）

按键触发式语音对话，完整闭环：

```
按下按键 → INMP441 录音(16kHz PCM 缓冲)
松开按键 → 停止录音 → 音频上传讯飞「一句话识别」→ 返回文字
        → 本地关键词意图解析:
            "开灯/关灯"        → 下发 51 控制继电器
            "开风扇/关风扇"    → 下发 51 控制 28BYJ-48 步进电机(风扇)
            "温度/几度/湿度"    → 取当前温湿度生成回复
            "开自动/关自动"     → 切换自动模式
            其他               → 闲聊随机回复
        → 回复文字 → 讯飞 TTS 合成 → MAX98357A 喇叭播放
        → 控制类指令同步走 UART 发给 51 执行
```

**接入方式**：因按键触发天然是"录一段→识别一段"，采用讯飞「一句话识别」（HTTP 上传短音频）而非 WebSocket 流式，ESP8266 代码量更小。TTS 采用讯飞在线语音合成接口。

**半双工规避**：ESP8266 的 I2S 为半双工（输入/输出不可同时）。按键触发使录音（输入）与播放（输出）在时间上错开，天然规避冲突。

### 5.2 环境监测

- DHT11 每 2s 采集一次温湿度，HC-SR04 超声波测距离
- 光敏电阻经 XPT2046（SPI ADC）采集，换算为光照强度 0~100%
- 数据本地 LCD1602 显示，并经 UART 上报 ESP8266 → 巴法云

### 5.3 自动环境调节（闭环反馈）

**自动模式**（语音/远程/网页可切换开关）：

```
【灯光】光照 < 30% → 环境暗 → 继电器吸合开灯
       光照 > 70% → 环境亮 → 继电器断开关灯

【风扇】温度 > 30℃ 或 湿度 > 80% → 步进电机转（风扇开）
       温度 < 27℃ 且 湿度 < 65% → 风扇停（带滞回防抖）
```

**开关灯闭环验证**（仅在自动模式执行）：

```
执行开灯 → 延时 500ms → 再读光敏值
  光强上升 > 3%   → 判定"开灯成功"
  光强无变化      → 判定"灯故障"，蜂鸣器报警 + 上报云端
关灯同理（光强应下降）
```

**报警**：温度>35 / <5、湿度>85 / <20、灯故障、距离<10cm 任一触发，蜂鸣器长鸣（只响蜂鸣器，不再联动执行器）。

此"执行动作后用传感器验证结果"的闭环反馈是课程设计的重要亮点。

### 5.4 远程控制

- **网页控制台**（本地 Flask + ECharts，打包为 smart_home.exe）：实时数值卡片 + 温度/湿度/光照三张独立曲线 + 语音交互面板（保留 10 次对话）+ 实体控制按钮（开灯/关灯/开风扇/关风扇/自动模式）
- 手机小程序「巴法云」订阅 `env` 主题查看实时数据、发布 `cmd` 主题下发指令
- 与语音控制共用 51 的同一套执行逻辑（本版无本地按键）

## 6. 通信协议

### 6.1 UART 协议（51 ↔ ESP8266）

采用 ASCII 冒号分隔帧，便于 51 解析：

| 方向 | 帧格式 | 说明 |
|------|--------|------|
| 51 → ESP8266 | `D:25:60` | 上报温湿度（温度:湿度，整数） |
| 51 → ESP8266 | `L:45` | 上报光照强度 0~100 |
| 51 → ESP8266 | `A:1` | 上报灯态（1 开 0 关） |
| 51 → ESP8266 | `R:120` | 上报距离 cm |
| 51 → ESP8266 | `W:1` | 上报报警状态（1 报警 0 正常） |
| 51 → ESP8266 | `F:1` | 上报风扇状态（1 开 0 关） |
| ESP8266 → 51 | `C:lamp:on` / `C:lamp:off` | 控制灯 |
| ESP8266 → 51 | `C:fan:on` / `C:fan:off` | 控制风扇（步进电机） |
| ESP8266 → 51 | `C:auto:1` / `C:auto:0` | 切换自动模式 |

**物理层**：波特率 9600。51 侧 P3.0/P3.1 硬串口，12MHz 晶振下用 Timer2 作波特率发生器（误差 ~0.16%）；ESP8266 侧用 SoftwareSerial（RX=D2、TX=D1），因 GPIO3 被 I2S 音频输出固定占用、硬件串口 RX 不可用（见 §8）。

电平匹配：51 为 5V TTL，ESP8266 为 3.3V。仅「51 TX → ESP8266 RX」一路需电阻分压；「ESP8266 TX(3.3V) → 51 RX」直连即可（3.3V 高于 51 输入高电平门限）。

### 6.2 MQTT 协议（ESP8266 ↔ 巴法云）

连接 `bemfa.com:9501`，ClientID=私钥（用户名/密码留空）。**主题为纯主题名，不带私钥前缀、不带 #**（巴法云按 ClientID 自动隔离命名空间；同一私钥允许多连接共存）。

| 主题 | 方向 | 内容 |
|------|------|------|
| `env` | 设备→云（发布） | JSON：`{"t":28,"h":60,"light":45,"lamp":1,"auto":1,"dist":120,"alarm":0,"fan":1}` |
| `cmd` | 云→设备（订阅） | `lamp:on` / `fan:on` / `auto:1`（也兼容 JSON `{"dev":"lamp","act":"on"}`） |

### 6.3 讯飞接口

- 语音识别：讯飞「一句话识别」（HTTP 上传音频，返回文字）
- 语音合成：讯飞在线语音合成（文本 → 音频流，返回 MP3/PCM）

## 7. 数据流

```
【上行】51 采集温湿度/光照/距离 → UART → ESP8266 → MQTT 发布 env → 巴法云 → 小程序/网页显示 + CSV 存档
【下行】小程序/网页 → 巴法云 cmd → ESP8266 MQTT 订阅 → UART → 51 执行继电器(灯)/步进电机(风扇)/自动模式
【语音】按键 → INMP441 → ESP8266 → 讯飞 ASR → 意图 → (控制: UART→51) + (回复: TTS→喇叭) + 网页显示对话
【反馈】51 自动模式开关灯后读光敏 → 验证结果 → 报警状态回传 ESP8266 → 上报云端
```

## 8. 关键技术点

1. **ESP8266 I2S 半双工**：通过按键触发分时复用输入/输出，规避同时读写冲突。
2. **I2S 引脚固定占用（重要）**：ESP8266 I2S 数据输出引脚芯片层面固定为 GPIO3(RX)，硬件串口 RX 被占用，51 通信改用 SoftwareSerial(D1/D2)；且 I2S 输入/输出时钟为不同固定引脚（输入 GPIO14/13/12，输出 GPIO15/2/3），麦克风与功放各用各的时钟，不能共享。
3. **电平匹配**：51(5V) 与 ESP8266(3.3V) UART 通信，仅「51→ESP8266」方向需分压，且分压电阻要取大值（10k+20k）——8051 准双向口高电平驱动弱，小阻值分压会拉垮信号。
4. **12MHz 晶振波特率**：9600 用 Timer1 有 ~8.5% 误差，改用 Timer2 作波特率发生器（~0.16%）。
5. **讯飞接口接入**：一句话识别 + 在线合成，需申请免费 AppID/APIKey（学生可免费）。
6. **光敏闭环反馈**：执行动作后延时采样验证结果，实现"感知—执行—验证"闭环。
7. **多控制源统一**：语音/网页/小程序三种控制源共用 51 同一套执行逻辑，避免冲突。
8. **XPT2046 SPI ADC**：新版板载 XPT2046（非 PCF8591 I2C），软 SPI 读通道0 采光敏，两点校准映射 0~100%。
9. **自动环境调节**：光控灯 + 温湿度控风扇（带滞回），多传感器联动。
10. **网页控制台**：Flask + ECharts 打包为单文件 exe，浏览器实时看曲线/控制，关闭网页后自动退出。

## 9. 后续扩展方向

- 增加 PIR 人体红外/超声波实现有人自动开灯、无人延时关灯
- 云端 NLU（自然语言理解）替代本地关键词匹配，支持更智能的闲聊
- 多房间多节点组网（多个 51 采集节点 + 一个 ESP8266 网关）
- 历史数据存储与图表分析（巴法云历史记录或自建服务器）
- 微信小程序定制界面（替代巴法云通用小程序）
