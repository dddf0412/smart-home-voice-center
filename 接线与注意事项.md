# NodeMCU v3 CH340 (ESP8266) 引脚分布表

## 左侧引脚（电源、模拟及 SPI 接口）

| 板载丝印 | 芯片引脚 | 功能/说明 |
| :--- | :--- | :--- |
| **TOUT** | **ADC0** | 模拟输入引脚，输入电压范围 0~3.3V |
| **GND** | GND | 接地 |
| **VOUT USB** | 3V3 | USB 接口提供的 3.3V 电源输出 |
| **IO10** | GPIO10 | 通常不用于通用 IO（SDIO 相关） |
| **IO9** | GPIO9 | 通常不用于通用 IO（SDIO 相关） |
| **IO8** | GPIO8 | **MOSI**（SPI 主出从入） |
| **IO7** | GPIO7 | **CS0**（SPI 片选） |
| **IO6** | GPIO6 | **MISO**（SPI 主入从出） |
| **IO5** | GPIO5 | **SCLK**（SPI 时钟）*注意：与右侧 D1 共用 GPIO5* |
| **GND** | GND | 接地 |
| **3v3** | 3V3 | 3.3V 电源输入/输出 |
| **EN** | EN | 芯片使能端（高电平有效），通常外接 10k 上拉电阻 |
| **RST** | RST | 复位引脚 |
| **GND** | GND | 接地 |
| **VIN** | VIN | 外部电源输入（通常接 5V，板载稳压器降压至 3.3V） |

## 右侧引脚（数字 IO、I2C、UART 及 HSPI 接口）

| 板载丝印 | 芯片引脚 | 功能/说明 |
| :--- | :--- | :--- |
| **D0** | **GPIO16** | **WAKE**（唤醒引脚） |
| **D1** | **GPIO5** | **SCL**（I2C 时钟） |
| **D2** | **GPIO4** | **SDA**（I2C 数据） |
| **D3** | **GPIO0** | **Flash** 相关，**CTS2**，**注意：不可下拉**（启动模式引脚，需上拉） |
| **D4** | **GPIO2** | **TXD 1**，**内置 LED**，**注意：不可下拉**（启动模式引脚，需上拉） |
| **3v3** | 3V3 | 3.3V 电源 |
| **GND** | GND | 接地 |
| **D5** | **GPIO14** | **HSPI SCK**（硬件 SPI 时钟） |
| **D6** | **GPIO12** | **HSPI MISO**（硬件 SPI 主入从出） |
| **D7** | **GPIO13** | **HSPI MOSI**（硬件 SPI 主出从入），**CTS0**，**RXD 2** |
| **D8** | **GPIO15** | **HSPI CS**，**RTS0**，**TXD 2**，**注意：不可上拉**（启动模式引脚，需下拉） |
| **RX** | **GPIO3** | **RXD 0**（UART 接收，常用于烧录程序） |
| **TX** | **GPIO1** | **TXD 0**（UART 发送，常用于烧录程序），**CTS1** |
| **GND** | GND | 接地 |
| **3v3** | 3V3 | 3.3V 电源 |

---

## ⚠️ 特别注意事项

- **启动引脚限制**：**GPIO0 (D3)**、**GPIO2 (D4)** 为**不可下拉**（上电时需保持高电平）；**GPIO15 (D8)** 为**不可上拉**（上电时需保持低电平）。如果这些引脚在启动时被错误拉低/拉高，会导致芯片无法正常启动或进入刷机模式。
- **内置 LED**：板载的蓝色 LED 连接在 **D4 (GPIO2)** 上，低电平点亮。
- **IO6~IO10**：这些引脚在 ESP8266 上通常用于连接内部 Flash 闪存，**不建议**作为普通的数字 IO 使用。
- 注：左侧的 IO5 与右侧的 D1 在物理上虽然是不同位置的引脚，但在芯片内部实际上是同一个 GPIO5，请根据您的接线需求选择使用。